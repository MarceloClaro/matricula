# Optional advanced dependencies for face recognition features
# These dependencies are heavy and provide advanced features like anti-spoofing
# The basic face recognition functionality (dlib, face-recognition) is now in requirements.txt

# To install these optional advanced features:
# 1. Ensure you have already installed the main requirements: pip install -r requirements.txt
# 2. Install these optional dependencies: pip install -r requirements-optional.txt

# Anti-spoofing (liveness detection) - Optional but recommended for production
# Using latest stable TensorFlow 2.x version
tensorflow>=2.15.0,<2.18.0

# Data augmentation - Optional but recommended for better training accuracy
imgaug==0.4.0
